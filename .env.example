DATABASE_URL="file:./dev.db"

# Green API (Phase 2 — not used yet, placeholders only)
# GREEN_API_INSTANCE_ID=
# GREEN_API_TOKEN=

# Optional: enables auth middleware for non-local deployments (see PRD D17)
# APP_PASSWORD=
